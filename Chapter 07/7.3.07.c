#include <stdio.h>

int main(void)
{
    int a = '\b', b = '\n', c = '\r', d = '\t';

    printf("Answer A) %o\n", a);
    printf("Answer B): %o\n", b);
    printf("Answer C): %o\n", c);
    printf("Answer D): %o\n", d);

    return 0;
}